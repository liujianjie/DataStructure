1.试试 move一个左值给一个右值，在使用右值之前，删除左值看看

试了：内置类型还是会关联两个，并且可以使用移后源对象

2.strvec什么时候 移后源对象会被销毁，是出了作用域就销毁，然后调用析构函数吗

在移动拷贝函数后不会销毁，还是要正常的销毁才会

3.倘若自赋值 移动赋值运算符，那么出作用域销毁右值，不就是会销毁吗？没有移给新的对象，并且移后源对象没有

处于销毁它是无害的

不会出移动赋值运算符后销毁，还是得在调用程序中正常销毁


4.重载和引用函数

引用限定符可以指定&或者 &&

&
const &

&&
const &&

&&
const & 为什么只有一个&

因为 & const &，const && 都会转换为const&

&

&&

const &

const &&

5。对一个左值修改为什么不可？

是不可 const